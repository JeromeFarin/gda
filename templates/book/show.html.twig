{% extends 'base.html.twig' %}

{% block title %}{{ book.title }}{% endblock %}

{% block body %}
    <h1>{{ book.title }}</h1>

    <p>{{ book.resume }}</p>

    <p>
        <i>{{ book.isbn }}</i>
    </p>
    <em>{{ book.price }}$</em>
{% endblock %}
